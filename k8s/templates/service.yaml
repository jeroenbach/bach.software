apiVersion: v1
kind: Service
metadata:
  name: bach-software-web
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: bach-software-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
