export default defineAppConfig({
  // Show different page configurations for the following pages (by content id)
  showGrayBackground: [30],
  showWhiteFooter: [20, 30],
  showShortFooter: [30],
  showNoBottomPadding: [20],
});
